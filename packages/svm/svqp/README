This directory contains two quadratic optimizers.

SVQP uses a hybrid gauss-seidel/conjugate-gradient.
It can perform arbitrary convex optimization.
It does not implement a kernel cache.

SVQP2 uses a gauss-seidel/smo approach.
It implements a nice kernel cache
and various shrinking strategies.
